services:
  terminal:
    build: .
    container_name: Terminal
    ports:
      - 87:3000
    volumes:
      - ./app/server.js:/app/server.js:ro
      - ./app/public:/app/public:ro
    pid: "host"
    privileged: true
    restart: always
